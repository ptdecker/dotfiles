# Use ctrl-a instead of ctrl-b as the prefix key
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# Use vim shortcuts
setw -g mode-keys vi

# Make ctrl-a r reload the config file
unbind r
bind r source-file ~/.tmux.conf

